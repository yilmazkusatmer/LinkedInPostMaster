# LinkedInPostMaster - Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Set default model (defaults to gpt-4o if not set)
# DEFAULT_MODEL=gpt-4o

# Optional: Enable debug mode (defaults to false)
# DEBUG=false

# Note for Streamlit Cloud:
# Don't use .env in production! Instead, configure secrets in:
# Streamlit Cloud Dashboard → App Settings → Secrets
# Format:
# OPENAI_API_KEY = "sk-your-actual-key"
